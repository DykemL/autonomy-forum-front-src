import { Alert, Container } from "@mui/material";

function NotFoundPage() {
  return (
    <Container>
      <Alert color="error">Путь не найден</Alert>
    </Container>
  )
}

export default NotFoundPage;